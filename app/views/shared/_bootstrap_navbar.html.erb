
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
<!--   <a class="navbar-brand" href="https://gngbase.com">
    <img src="<%#= image_url 'logo.png' %>" width="45" height="45" alt="logo">
  </a> -->
  <%= link_to root_path, class: "navbar-brand" do %>
    <%= image_tag "logo.png", height: 45, width:45, alt: 'logo' %>
  <% end %>
  <%= render 'shared/search_bar' %>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav ml-auto">
  <% if user_signed_in? %>
        <a class="nav-item nav-link disabled" href="#">Schedule</a>
        <%= link_to 'Messages', conversations_path, class: "nav-item nav-link" %>
        <a class="nav-item nav-link disabled" href="#">Membership</a>
        <%= link_to 'Saved', bookmarks_path, class: "nav-item nav-link"  %>
        <div class="dropdown">
        <% if !current_user.image.blank? %>
          <%= image_tag current_user.image, class: "avatar-small dropdown-toggle", id: "dropdownMenuButton", "data-toggle" => "dropdown"%>
        <% else %>
          <button class="btn btn-secondary btn-sm dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Account
          </button>
        <% end %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
            <% if !current_user.name.blank? %>
              <p class="text-center"><%= current_user.name %></p>
              <div class="dropdown-divider"></div>
              <% end %>
            <%= link_to t(".log_out", default: "Logout"), destroy_user_session_path, class: "dropdown-item" %>
          </div>
        </div>
  <% else %>
      <!-- Login link (when logged out) -->
      <%= link_to t(".sign_in", default: "Login"), new_user_session_path, class: "navbar-item nav-link" %>
      <%= link_to t(".sign_up", default: "Sign up"), new_user_registration_path, class: "navbar-item nav-link" %>
  <% end %>
      </div>
      </div>
</nav>

